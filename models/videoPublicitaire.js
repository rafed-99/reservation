const mongoose = require('mongoose');

const videoPublicitaireSchema = new mongoose.Schema({



})

const videoPublicitaire = mongoose.model("videoPublicitaire",videoPublicitaireSchema);
module.exports = videoPublicitaire;